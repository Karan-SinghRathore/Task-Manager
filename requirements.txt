PySide6>=6.6.0
pymongo>=4.6.0
Windows-Toasts>=1.3.0
pyinstaller>=6.3.0

# Only needed to (re)generate assets/icon.ico from assets/icon.svg
# via scripts/generate_icon.py — not required to run or build the app.
pillow>=10.0.0
